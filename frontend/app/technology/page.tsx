const steps = [
  ["1", "Client-side Encryption", "Your secret is encrypted inside your browser before a transaction is prepared."],
  ["2", "Encrypted Data Sent On-chain", "The blockchain receives an encrypted handle, not the readable value you selected."],
  ["3", "Confidential Computation by Inco", "Inco processes the protected values inside a trusted execution environment."],
  ["4", "Attested Decryption", "Covalidators sign an attestation confirming that the revealed result came from the correct computation."],
  ["5", "Only Player Can Reveal Results", "Access permissions keep private results scoped to the player's connected wallet."],
];
export default function TechnologyPage() { return <main className="page-shell"><div className="mx-auto max-w-3xl text-center"><p className="eyebrow">Technology</p><h1 className="mt-4 page-title">Confidential gameplay, explained simply.</h1><p className="page-description mx-auto">NinjaInco keeps selected game data private while still letting smart contracts use it. No cryptography knowledge required.</p></div><div className="mx-auto mt-14 max-w-3xl">{steps.map(([n,title,copy], index) => <div key={title}><article className="surface surface-hover grid gap-5 p-6 sm:grid-cols-[64px_1fr] sm:p-8"><div className="grid h-16 w-16 place-items-center rounded-full border border-foreground/20 bg-foreground text-lg font-semibold text-background">{n}</div><div><h2 className="text-xl font-semibold">{title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground sm:text-base">{copy}</p></div></article>{index < steps.length-1 && <div aria-hidden="true" className="py-3 text-center text-2xl text-muted-foreground">↓</div>}</div>)}</div><section className="surface mx-auto mt-14 max-w-3xl bg-gradient-to-br from-card to-background p-7 text-center sm:p-10"><h2 className="text-2xl font-semibold">What stays public?</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Your wallet, ninja name, village, and earned rank are public. Techniques, attributes, duel outcomes before reveal, experience, and promotion eligibility remain confidential.</p></section></main>; }
